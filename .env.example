# 根目录模板：复制为 .env 后填入真实密码（.env 已在 .gitignore，勿提交）
#
#   cp .env.example .env

# PostgreSQL（与 postgres-server 容器一致）
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=onemini

# MinIO（与 minio-server 容器 MINIO_ROOT_* 一致）
MINIO_ACCESS_KEY=changeme
MINIO_SECRET_KEY=changeme
